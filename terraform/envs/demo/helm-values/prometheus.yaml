server:
  persistentVolume:
    enabled: false
  resources:
    limits:
      memory: 1536Mi
    requests:
      cpu: 200m
      memory: 768Mi

alertmanager:
  enabled: false

kube-state-metrics:
  enabled: false

prometheus-node-exporter:
  enabled: false

prometheus-pushgateway:
  enabled: false
